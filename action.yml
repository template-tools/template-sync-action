name: 'Template Sync'
description: 'sync repository with its template'
inputs:
  template:
    description: 'name of the template repository'
    required: false 
runs:
  using: 'node20'
  main: 'dist/index.mjs'
